name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:

  build:

    runs-on: ubuntu-latest
#dckr_pat_3cPyUe9F4Nn9A5CGwe0NrhNh-j8
    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
    - name: Build with Gradle
      uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
      with:
        arguments: build

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_PASSWORD }}
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag cedje/teammanager:team

    - name: Push image
      run: docker push cedje/teammanager:team

#    - name: Build with Maven
#      run: mvn package
#
#    - name: Run tests
#      run: mvn test
#    - name: Run tests
#      run: docker run cedje/teammanager:team sh -c "cd src/test/java/com/example/teammanagerapi/TeamManagerApiApplicationTest"
