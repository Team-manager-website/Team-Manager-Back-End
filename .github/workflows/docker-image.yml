#name: Docker Image CI
#
#on:
#  push:
#    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]
#
#jobs:
#
#  build:
#
#    runs-on: ubuntu-latest
##dckr_pat_3cPyUe9F4Nn9A5CGwe0NrhNh-j8
#    steps:
#    - uses: actions/checkout@v3
##    - name: Login to Docker Hub
##      uses: docker/login-action@v2
##      with:
##        username: ${{ secrets.DOCKERHUB_USERNAME }}
##        password: ${{ secrets.DOCKERHUB_TOKEN }}
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
#
#
##    - name: Build with Maven
##      run: mvn package
##
##    - name: Run tests
##      run: mvn test
#    - name: Run tests
#      run: docker run my-image-name:$(date +%s) sh -c "cd src/test/java/com/example/teammanagerapi/TeamManagerApiApplicationTest"

name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: false
          tags: your-image-name:${{ github.sha }}

      - name: Test Docker image
        run: docker run your-image-name:${{ github.sha }} npm test

      - name: Deploy to production
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker push your-image-name:${{ github.sha }}